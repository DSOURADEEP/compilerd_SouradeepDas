rename the folder as compilerd_SouradeepDas which is my name
use the command  "npm install react-scripts --save-dev" before running the "npm start" command for the compilerd-backend
use the command "node server.js" to run the backend.  
